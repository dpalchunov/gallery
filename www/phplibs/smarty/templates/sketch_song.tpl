    <div class="sketch song ui-widget-content"
         ratio="{{{$ratio}}}" song_id="{{{$song_id}}}"
         left ="{{{$left}}}" top ="{{{$top}}}" width= "{{{$width}}}"
         song_path={{{$song_path}}}
         style="left:{{{$css_left}}};width:{{{$css_width}}};">
        <table class="song_desc">
            <tr><td align="left"><p style="text-indent:20px" id="{{{$song_desc_id}}}">{{{$songDescription}}}</p></td></tr>
        </table>
    </div>

